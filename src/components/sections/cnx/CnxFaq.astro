---
import { CONECTIVIDAD } from "../../../data/conectividad.js";
import Icon from "../../ui/Icon.astro";

const { faqs } = CONECTIVIDAD;

// Mismo reparto equitativo en dos columnas que el resto de páginas.
const half = Math.ceil(faqs.length / 2);
const faqColumns = [faqs.slice(0, half), faqs.slice(half)];
---

<section class="section">
  <div class="container">
    <div class="section-head reveal">
      <span class="eyebrow">Preguntas frecuentes</span>
      <h2 class="h2">Dudas habituales sobre conectividad</h2>
    </div>

    <div class="faq-grid reveal" style="transition-delay:.1s">
      {faqColumns.map((column) => (
        <div class="faq-list">
          {column.map((f) => (
            <details class="faq-item glass-card">
              <summary class="faq-q">
                {f.q}
                <Icon name="chevron-down" />
              </summary>
              <div class="faq-a">{f.a}</div>
            </details>
          ))}
        </div>
      ))}
    </div>
  </div>
</section>
