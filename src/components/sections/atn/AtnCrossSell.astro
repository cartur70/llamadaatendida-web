---
import { ATENCION_247 } from "../../../data/atencion-24-7.js";
import Button from "../../ui/Button.astro";

const { crossSell } = ATENCION_247;
---

<section class="section">
  <div class="container">
    <div class="atn-crosssell reveal">
      <h2 class="h2">{crossSell.title}</h2>
      <p>{crossSell.subtitle}</p>
      <Button href={crossSell.href} variant="primary" icon="shield">{crossSell.cta}</Button>
    </div>
  </div>
</section>
